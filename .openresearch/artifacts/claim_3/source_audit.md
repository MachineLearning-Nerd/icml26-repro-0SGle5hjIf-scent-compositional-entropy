# Claim 3 source audit

Lemma 3.3 states: under the bounded-risk premise of Assumption 3.2(ii), if
`nu_0 in [c0,c1]^n`, then `nu_{i,t} in [c0,c1]` for every coordinate `i` and
every `t >= 1`; the two variance terms are finite. The imported claim omits the
initialization and bounded-risk preconditions.

Equation (7) makes `exp(nu_t)` a convex combination of `exp(nu_{t-1})` and
`exp(s_t)`, which is the algebraic invariant tested here.

