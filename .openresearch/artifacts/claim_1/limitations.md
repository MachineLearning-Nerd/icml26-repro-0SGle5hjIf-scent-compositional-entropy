# Claim 1 limitations

This is an exact scalar update check plus a declared finite stress grid, not a
claim that arbitrary hardware or non-finite inputs can never fail. The
independent numerical argmin uses moderate inputs; extreme inputs are checked
against arbitrary-precision arithmetic instead.

